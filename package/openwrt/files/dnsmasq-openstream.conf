# Auto-fill openstream_hls when clients use router dnsmasq (not DoH).
# Только HLS CDN — НЕ /twitch.tv/ (иначе www/gql → MITM без CA = сайт мёртв).
# Requires dnsmasq-full with nftset support (OpenWrt 23+).

nftset=/ttvnw.net/4#inet#openstream#openstream_hls
nftset=/jtvnw.net/4#inet#openstream#openstream_hls
nftset=/live-video.net/4#inet#openstream#openstream_hls
